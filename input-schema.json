{
    "additionalProperties": false,
    "required": [
	    "runid",
	    "gl",
	    "poppy",
	    "function"
    ],
    "properties": {
        "runid": {
            "additionalProperties": false,
            "type": "string",
            "title": "Unique id used in titles and folders"
	},
    "gl": {
        "additionalProperties": false,
        "type": "object",
        "format": "globus-collection",
        "title": "Great Lakes ID and path",
        "required": [
    	    "id",
    	    "path"
        ],
        "properties":{
    	    "id": {
                "title": "Great Lakes endpoint",
    		    "type": "string",
    		    "format": "uuid"
    	    },
    	    "path": {
                "title": "Great Lakes path",
    		    "type": "string"
    	    }
        }
    },
    "poppy": {
        "additionalProperties": false,
        "type": "object",
        "format": "globus-collection",
        "title": "Poppy ID and path",
        "required": [
    	    "id",
    	    "path"
        ],
        "properties":{
    	    "id": {
		        "title": "Poppy endpoint",
    		    "type": "string",
    		    "format": "uuid"
    	    },
    	    "path": {
		        "title": "Poppy path",
    		    "type": "string"
    	    }
        }
    },
	"function": {
            "additionalProperties": false,
            "type": "object",
	    "title": "funcx inputs",
	    "required": [
		    "endpoint",
		    "func"
	    ],
	    "properties": {
		    "endpoint": {
			    "title": "UUID of funcx endpoint to run on",
			    "type": "string",
			    "format": "uuid"
		    },
		    "func": {
			    "title": "UUID of funcx functoin to call",
			    "type": "string",
			    "format": "uuid"
		    }
	    }
	}
    }
}
